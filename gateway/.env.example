# Gateway Environment Variables
# Both variables are REQUIRED — the app will show an error page if they are missing.

VITE_SCHOLAR_SYNC_URL=https://your-scholar-sync-domain.com
VITE_SCHOLAR_TRACK_URL=https://your-scholar-track-domain.com
